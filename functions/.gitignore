node_modules/
# Slash initial obligatoire : « lib/ » seul ignorerait AUSSI src/lib/, où vivent
# auth.ts, firebase.ts, errors.ts et refs.ts. Ici on ne vise que la sortie de tsc.
/lib/
build-test/
.env
*.log
.firebase/
